{
  "name": "azd-catalog-validator-action",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic validation for repository-owned azd catalog metadata.",
  "scripts": {
    "sync-schema": "node scripts/sync-schema.mjs",
    "build": "npm run sync-schema && ncc build src/index.js --out dist --minify && node scripts/write-manifest.mjs",
    "test": "npm run sync-schema && node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@actions/core": "2.0.3",
    "ajv": "8.20.0",
    "jsonc-parser": "3.3.1"
  },
  "devDependencies": {
    "@vercel/ncc": "0.45.0"
  }
}
